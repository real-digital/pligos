version: "1"

types: ["../../flavors/types.yaml"]

contexts:
  - name: default
    flavor: ../../flavors/webservice
    configs: ["config/app.yaml", "script/helloworld.sh"]
    secrets: ["secrets/credentials.yaml"]
